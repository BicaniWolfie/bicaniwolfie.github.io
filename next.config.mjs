/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['en', 'dk'],
        defaultLocale: 'en',
        localeDetection: false
    }
};

export default nextConfig;
